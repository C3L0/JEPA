[project]
name = "jepa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.8.4",
    "equinox>=0.13.6",
    "jax>=0.9.2",
    "lerobot>=0.5.0",
]
